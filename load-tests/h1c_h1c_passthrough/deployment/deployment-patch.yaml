apiVersion: "apps/v1"
kind: Deployment
metadata:
  name: no-name
spec:
  template:
    metadata:
      labels:
        logs: "true"
    spec:
      containers:
      - name: "h1c-h1c-passthr-deployment"
        imagePullPolicy: Always

